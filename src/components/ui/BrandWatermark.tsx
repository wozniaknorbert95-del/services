import './brand-logo.css';

/** Decorative hero watermark — graphic logo at low opacity; hidden on mobile. */
export default function BrandWatermark() {
  return <div className="qf-brand-watermark" aria-hidden="true" />;
}
