# Phase 1 — Home Page Deep Rebuild

**Date:** 2026-05-31
**Scope:** Full Home page mapped 1:1 to `Home — Copy & Wireframe (Filar 2).md`.

---

## What was done

1. **7 section components** created in `src/components/home/`:
   - `HeroSection.tsx` — H1 + terminal panel mock + status bar
   - `PainGrid.tsx` — 4 pain tiles with icons and links
   - `SpearheadSpotlight.tsx` — Inbox Killer emphasis with `.qf-panel--spearhead` glow
   - `LadderTeaser.tsx` — 3 up-sell cards (Web, Funnel, Lead Magnet)
   - `WhyThisWorks.tsx` — 3 moat pillars (live system / AI workforce / HITL)
   - `ResultsTeaser.tsx` — 2 mini cases with honest `[X]` placeholders
   - `FinalCtaBand.tsx` — Paid Discovery CTA band with risk reversal
2. **`src/app/page.tsx`** — composes all 7 sections + `Metadata` export
3. **OG image** — `/public/og/home.svg` updated to Quietforge branding
4. **Old components deleted** — `Hero.tsx`, `PainPoints.tsx`, `HowItWorks.tsx`, `CaseStudies.tsx`, `CTAFinal.tsx`
5. **Type fix** — `motion.ts` `ease: 'easeOut' as const` for Framer Motion strict types
6. **Build gate** — `npm run build` + `npm run typecheck` pass with zero errors

---

## Copy fidelity

All copy pulled verbatim from `Home — Copy & Wireframe (Filar 2).md`:
- H1: "Your website and back-office, running on autopilot."
- Pain tiles, Spearhead bullets, Ladder cards, Why pillars, Results cases, Final CTA body.

---

## Design system usage

- `--qf-*` tokens throughout
- `qf-panel--spearhead` for Inbox Killer emphasis
- Terminal panel mock (`qf-panel__bar` + `qf-code`) in Hero
- `qf-statusbar` under Hero
- Sharp 2px radius, amber accent, monospace font

---

## Next step

**Phase 2 — Solutions Hub + Inbox Killer** (sessions 4–5).
