# README — Section 2 „How I work (process)"

## Purpose
Prove maturity: a named architect shows a repeatable method. Maps your real 12-step internal
workflow onto 5 client-readable phases (Map → Architect → Build → Verify → Hand over).

## Where to place
On the home page, **after About (section 1), before Trust & Safety (section 3)**.

## How to implement
1. Add markup from `section.md`.
2. Map `qf-*` onto repo tokens (numbered timeline style — see preview.html).
3. Keep the `planner → coder → tester → review` phrasing — it echoes the terminal snippets
   already on the site (`classify → plan → diff → approve`), reinforcing consistency.

## Rules
- „paid first step" tag on phase 1 links conceptually to the Automation Map (don't remove).
- The closing note ties the process to the real ecosystem — keep it, it's the credibility hook.
- Deliverables per phase must stay — they signal „you get artefacts, not vibes".

## Definition of Done
- [ ] 5 phases render as a numbered timeline, responsive.
- [ ] Sits between About and Trust & Safety.
- [ ] Tags (paid first step / human-in-the-loop / no lock-in) visible.
